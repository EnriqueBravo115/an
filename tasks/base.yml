- name: install base
  become: true
  pacman:
    name:
      - xorg-xrandr
      - man
      - unzip
      - zip
      - docker
      - curl
      - openssh
      - tar
    state: present
    update_cache: true
